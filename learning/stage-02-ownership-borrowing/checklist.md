# 第 2 阶段 Checklist

把下面内容学完，再进入下一阶段。

## ownership

- [x] 知道什么是所有权
- [x] 知道离开作用域后值会被释放
- [x] 知道赋值、传参时可能发生 move
- [x] 知道哪些类型通常实现了 `Copy`

## borrowing

- [x] 知道什么是不可变借用 `&T`
- [x] 知道什么是可变借用 `&mut T`
- [x] 知道一个值可以有多个不可变借用
- [x] 知道同一时间只能有一个可变借用
- [x] 知道为什么可变借用和不可变借用不能同时存在

## String / &str / slice

- [x] 知道 `String` 拥有数据，`&str` 是借用的字符串切片
- [x] 知道什么时候函数参数该写 `String`
- [x] 知道什么时候函数参数该写 `&str`
- [x] 会使用字符串 slice，如 `&s[..]`
- [x] 知道 slice 不拥有数据

## 函数与设计感觉

- [ ] 能判断函数是在拿走所有权还是借用数据
- [ ] 能把一个函数改成借用参数而不是获取所有权
- [ ] 能解释什么时候需要 `clone`
- [ ] 知道 `clone` 不是默认解法

## 实战练习

- [ ] 完成 `01_ownership.rs`
- [ ] 完成 `02_borrowing.rs`
- [ ] 完成 `03_references_and_mut.rs`
- [ ] 完成 `04_slice.rs`
- [ ] 完成猜数字游戏升级版

## 自测问题

- [ ] 为什么 `String` 赋值给另一个变量后原变量通常不能再用
- [ ] `&String` 和 `&str` 有什么常见区别
- [ ] 为什么一个函数参数如果只读，通常优先考虑 `&str`
- [ ] 为什么 Rust 要限制可变借用数量
- [ ] `clone` 解决了什么问题，又带来了什么代价
