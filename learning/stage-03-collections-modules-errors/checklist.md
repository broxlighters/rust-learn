# 第 3 阶段 Checklist

把下面内容学完，再进入下一阶段。

## Vec

- [x] 会创建空 `Vec` 和带初始值的 `Vec`
- [x] 会使用 `push` 新增元素
- [x] 会用 `for` 遍历 `Vec`
- [x] 知道索引和 `get` 的区别

## HashMap

- [x] 会插入键值对
- [x] 会根据 key 读取 value
- [x] 会更新已存在的值
- [x] 能用 `HashMap` 做简单计数

## 模块与 use

- [x] 知道 `mod` 用来声明模块
- [x] 知道 `pub` 决定哪些内容能暴露出去
- [x] 会用 `use` 简化路径
- [x] 能把函数和结构体拆到更清晰的位置

## 错误处理

- [x] 知道 `panic!` 适合不可恢复的问题
- [x] 会定义 `Result<T, E>` 返回值
- [x] 会用 `match` 处理 `Result`
- [x] 会用 `?` 传播错误

## 文件读写

- [ ] 会读取文本文件
- [ ] 会把字符串写入本地文件
- [ ] 知道读取失败时应该如何返回错误

## 实战练习

- [ ] 完成 `01_vec.rs`
- [ ] 完成 `02_hashmap.rs`
- [ ] 完成 `03_modules.rs`
- [ ] 完成 `04_result_and_question.rs`
- [ ] 完成命令行 Todo 程序

## 自测问题

- [ ] `Vec` 和数组的主要区别是什么
- [ ] 为什么 `HashMap` 适合做查找和统计
- [ ] 为什么有些函数应该返回 `Result` 而不是直接 `panic!`
- [ ] `?` 到底帮你省掉了什么代码
- [ ] 为什么 Todo 项目需要拆成多个函数或模块
