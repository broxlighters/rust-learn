# 第 1 阶段 Checklist

把下面内容学完，再进入下一阶段。

## 基础语法

- [x] 知道 `let` 和 `const` 的区别
- [x] 会使用可变变量 `mut`
- [x] 知道 shadowing 是什么
- [x] 了解整数、浮点数、布尔、字符、字符串

## 函数与表达式

- [x] 会定义函数
- [x] 会写带参数和返回值的函数
- [x] 理解“表达式有值，语句没有值”
- [x] 知道块表达式可以返回结果

## 控制流

- [x] 会写 `if / else`
- [x] 会写 `match`
- [x] 会写 `loop`
- [x] 会写 `while`
- [x] 会写 `for`

## 复合类型

- [x] 会定义和使用数组
- [x] 会定义和使用元组
- [x] 会定义和实例化结构体
- [x] 会定义和匹配枚举

## 基础错误处理

- [*] 知道 `Option` 用来表示“可能有值，也可能没有值”
- [*] 知道 `Result` 用来表示“成功或失败”
- [*] 会用 `match` 处理 `Option`
- [*] 会用 `match` 处理 `Result`

## 实战练习

- [ ] 完成温度转换器
- [ ] 完成斐波那契数列
- [ ] 完成简单计算器
- [ ] 至少手写一遍而不是只修改示例

## 自测问题

- [ ] 为什么 `if` 在 Rust 里是表达式
- [ ] `match` 和一串 `if / else` 的区别是什么
- [ ] 数组和元组的使用场景有什么不同
- [ ] 结构体和枚举分别适合表达什么
- [ ] `Option` 和 `Result` 分别解决什么问题
