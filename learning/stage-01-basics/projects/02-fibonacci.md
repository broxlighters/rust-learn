# 项目 2：斐波那契数列

## 目标

写一个程序，输出第 `n` 个斐波那契数，或者输出前 `n` 项。

## 最低要求

- 能输出第 `n` 个值
- 使用循环完成，不要一开始就写递归

## 建议拆分

1. 先定义 `n`
2. 用两个变量记录前两项
3. 用 `for` 或 `while` 逐步迭代
4. 打印最终结果

## 进阶练习

- 输出前 `n` 项而不是只输出一个值
- 把逻辑封装成函数
- 尝试处理 `n = 0` 和 `n = 1`

## 训练点

- 循环
- 可变变量
- 整数类型
- 边界条件
