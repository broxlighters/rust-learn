# 项目 3：简单计算器

## 目标

写一个支持加减乘除的简单计算器。

## 最低要求

- 先写死两个数字和一个运算符
- 根据运算符输出结果

## 建议拆分

1. 定义 `left`、`right`、`operator`
2. 用 `match` 匹配 `+`、`-`、`*`、`/`
3. 输出运算结果
4. 处理除数为 0 的情况

## 进阶练习

- 把逻辑封装成 `calculate` 函数
- 返回 `Result`
- 让非法运算符给出错误提示

## 训练点

- `match`
- 分支逻辑
- 函数设计
- `Result` 的基础使用
